# Kuantaz Challenge


## Installation

first create .env in directory docker and after copy variables of env.template and replace values

Compile docker image

```bash
make build
```

Run the project

```bash
make up
```

## Swagger API

```bash
http://localhost:8000/openapi/swagger
```
